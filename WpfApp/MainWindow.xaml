<Window x:Class="WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp"
        mc:Ignorable="d"
        Title="WpfApp" Height="600" Width="800">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0,0,0,8" Grid.Row="0" >
            <Button Content="Импорт CSV" Command="{Binding LoadCommand}" Margin="2"/>
            <Button Content="Группировать данные" Command="{Binding GroupCommand}" Margin="2"/>
            <Button Content="Проверить данные" Command="{Binding ValidateCommand}" Margin="2"/>
            <Button Content="Сформировать сводку" Command="{Binding GenerateSummaryCommand}" Margin="2"/>
            <Button Content="Выгрузить данные в json" Command="{Binding ExportCommand}" Margin="2"/>
            <TextBlock Text="{Binding Status}" Margin="10,0" VerticalAlignment="Center"/>
            <!--<TextBlock Text="Путь к файлу:" VerticalAlignment="Center" Margin="8,0,0,0"/>
            <TextBox x:Name="txtFilePath" Width="230" Margin="6,0" IsReadOnly="True"/>-->
        </StackPanel>
        
        <Grid Grid.Row="1">
            <!-- Загруженные данные из файла csv -->
            <GroupBox Header="Загруженные данные из файла csv" Grid.Column="1" VerticalAlignment="Top" Height="120">
                <DataGrid ItemsSource="{Binding Items}" AutoGenerateColumns="True" 
              IsReadOnly="True" AlternatingRowBackground="LightGray" />
            </GroupBox>
        </Grid>

        <Grid Grid.Row="2">
            <!-- Сгруппированные данные -->
            <GroupBox Header="Сгруппированные данные" Grid.Column="1" VerticalAlignment="Top" Height="120">
                <DataGrid ItemsSource="{Binding Groups}" AutoGenerateColumns="True" 
                 IsReadOnly="True" AlternatingRowBackground="LightGray" />
            </GroupBox>
        </Grid>

        <!-- Раздел ошибок -->
        <Grid Grid.Row="3">
            <GroupBox Header="Ошибки" Grid.Column="1" VerticalAlignment="Top" Height="120">
                <DataGrid ItemsSource="{Binding Errors}" AutoGenerateColumns="True" 
                IsReadOnly="True" AlternatingRowBackground="LightGray" />
            </GroupBox>
        </Grid>

        <Grid Grid.Row="4">
            <!-- Сводная информация по скважинам -->
            <GroupBox Header="Сводная информация по скважинам" Grid.Column="1" VerticalAlignment="Top" Height="150">
                <DataGrid ItemsSource="{Binding Summary}" AutoGenerateColumns="True" 
                      IsReadOnly="True" AlternatingRowBackground="LightGray" />
            </GroupBox>
        </Grid>

        

        <!-- Действия -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Grid.Row="4" Margin="0,6,0,0">
            <!-- <Button Content="Выход" Width="110" Click="BtnExit_Click"/>-->
        </StackPanel>

    </Grid>
</Window>
